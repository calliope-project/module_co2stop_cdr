Per-shape scenarios for {{ snakemake.wildcards.dataset }} - {{ snakemake.wildcards.cdr_group }}.

This is done using the ``imputation:bounds`` section of the configuration.
**IMPORTANT:** traps can still be removed during aggregation for cases where their capacity is already contained within kept storage unit aquifers.
